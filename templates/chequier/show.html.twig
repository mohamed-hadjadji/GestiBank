{% extends 'base.html.twig' %}

{% block title %}Chequier{% endblock %}

{% block body %}
    <h1>Chequier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chequier.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ chequier.quantite }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ chequier.etat }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ chequier.date ? chequier.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Compte</th>
                <td>{{ chequier.compte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chequier_index') }}">back to list</a>

    <a href="{{ path('app_chequier_edit', {'id': chequier.id}) }}">edit</a>

    {{ include('chequier/_delete_form.html.twig') }}
{% endblock %}
