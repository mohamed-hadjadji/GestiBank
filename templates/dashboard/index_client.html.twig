{% extends 'board.html.twig' %}

{% block title %}Dashboard - Client{% endblock %}

{% block dash_title %}
	<a class="navbar-brand ps-3" href="{{path('app_dashboard_client')}}">Client Dashboard</a>
{% endblock %}
{% block navigation %}
    <div class="sb-sidenav-menu-heading">Paramètres Compte</div>
	<!--Gestion comptes-->
	<a class="nav-link collapsed" href="" data-bs-toggle="collapse" data-bs-target="#collapseLayouts" aria-expanded="false" aria-controls="collapseLayouts">
		<div class="sb-nav-link-icon">
			<i class="fas fa-columns"></i>
		</div>
		Comptes bancaires
		<div class="sb-sidenav-collapse-arrow">
			<i class="fas fa-angle-down"></i>
		</div>
	</a>
	<div class="collapse" id="collapseLayouts" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
		<nav class="sb-sidenav-menu-nested nav">
			<a class="nav-link" href="{{path('app_client_compte', {'id': app.user.id })}}">Mes comptes</a>
			<a class="nav-link" href="">Historiques Transactions</a>
            <a class="nav-link" href="">Virements</a>
		</nav>
	</div>

    <div class="sb-sidenav-menu-heading">Autres services</div>
	<a class="nav-link collapsed" href="" data-bs-toggle="collapse" data-bs-target="#collapseLayout" aria-expanded="false" aria-controls="collapseLayouts">
		<div class="sb-nav-link-icon">
			<i class="fas fa-columns"></i>
		</div>
		Mon profil
		<div class="sb-sidenav-collapse-arrow">
			<i class="fas fa-angle-down"></i>
		</div>
	</a>
	<div class="collapse" id="collapseLayout" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
		<nav class="sb-sidenav-menu-nested nav">
			<a class="nav-link" href="{{ path('app_client_show', {'id': app.user.id }) }}">Mes infos</a>
			<a class="nav-link" href="{{ path('app_client_pass', {'id': app.user.id }) }}">Changer mot de passe</a>
		</nav>
	</div>
    <a class="nav-link" href="">
		<div class="sb-nav-link-icon">
			<i class="fas fa-table"></i>
		</div>
		Demander un carnet de chèque
	</a>

</div></div><div class="sb-sidenav-footer">
<div class="small">Nous sommes le
	{{ 'now' | date('m-d') }}</div></div></nav></div><div id="layoutSidenav_content"><main><div class="container-fluid px-4">
{% for message in app.flashes('info') %}
	<div class="alert alert-info">
		{{ message }}
	</div>
{% endfor %}
{% for message in app.flashes('success') %}
	<div class="alert alert-success">
		{{ message }}
	</div>
{% endfor %}
{% for message in app.flashes('error') %}
	<div class="alert alert-danger">
		{{ message }}
	</div>
{% endfor %}{% endblock %}